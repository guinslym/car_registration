<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Car Registration" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Car Registration" %>">
    <%= stylesheet_link_tag 'all', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= render 'shared/navbar' %>
    <%= render 'layouts/messages' %>
    <main role="main">
      <span id="hello"></span>
       <%= yield %>
    </main>
    <section id="footer">
    <%= render 'shared/footer' %>
    </section>
  </body>
</html>
